VITE_API_URL=http://localhost:4000/api/v1
# PLT-1: v1 runs single-tenant (SPEC.md §2 goal 5); replace with the design partner's
# tenantId once provisioned. Subdomain/slug-based resolution arrives with PLT-8.
VITE_DEFAULT_TENANT_ID=
