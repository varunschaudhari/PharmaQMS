// Polymorphic identities the platform services key documents by.
export const DOCUMENT_ENTITY_TYPE = 'Document';
// DOC-3: each VERSION runs its own approval workflow instance (one instance per entity).
export const DOCUMENT_VERSION_ENTITY_TYPE = 'DocumentVersion';
