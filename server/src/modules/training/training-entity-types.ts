// Polymorphic identity Training's own records are keyed by (e-signatures, audit events).
export const TRAINING_ASSIGNMENT_ENTITY_TYPE = 'TrainingAssignment';
// TRN-6: the question-bank entity's own identity for audit events (upsert/approve).
export const TRAINING_ASSESSMENT_ENTITY_TYPE = 'TrainingAssessment';
