// TRN-6: an assessment (question bank) attached to one document VERSION. Draft while QA is
// authoring/editing questions; Approved once QA has reviewed it and it becomes usable by
// trainees — mirrors the "explicit QA review step" a carried-forward assessment also requires.
export enum TrainingAssessmentStatus {
  DRAFT = 'draft',
  APPROVED = 'approved',
}
