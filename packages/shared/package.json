{
  "name": "@pharmaqms/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Shared types, enums, and zod schemas used by both server and client",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "echo \"@pharmaqms/shared: no tests yet\" && exit 0"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
