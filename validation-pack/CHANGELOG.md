# PharmaQMS Validation Pack — Change Log

Per-release change log with impact assessment (SPEC.md §5.5). One row per release; requirement IDs must match the traceability matrix.

| Date | Requirement IDs | Description | Impact |
|---|---|---|---|
| 2026-07-11 | N/A (SPEC §8 Phase 0, step 1 — repo scaffold) | Initial monorepo scaffold: npm workspaces (server/client/packages/shared), NestJS server shell with strict TS, Vite+React+Tailwind client shell, Jest+Supertest+mongodb-memory-server, Vitest+RTL, shared ESLint/Prettier config, docker-compose (Mongo+Redis). No business logic implemented. | new feature — no impact on existing validated functions (no prior release exists) |
| 2026-07-11 | PLT-1 | Auth & RBAC: tenant-scoped User/Role Mongoose schemas, login with JWT access+refresh tokens, refresh-token rotation (tokenVersion invalidation), configurable account lockout after N failed attempts, configurable password policy (complexity validator + expiry flag), permission-matrix enum (view/create/edit/review/approve/admin per module) with PermissionsGuard + @RequirePermission(), JwtAuthGuard/TenantGuard, global AppException/AllExceptionsFilter error shaping, ZodValidationPipe. Client: AuthContext, axios instance with refresh-on-401 interceptor, LoginPage, ProtectedRoute. 29 server tests + 3 client tests, all passing. | new feature — no impact on existing validated functions. Audit-trail hooks for failed-login/lockout events are stubbed with `TODO(PLT-2)` pending the audit service. |
| 2026-07-11 | PLT-2 (+ PLT-1 follow-up) | Audit Trail: append-only `auditEvents` Mongoose model (create/find only — every update/delete path rejected at the schema level via pre-hooks), field-level old→new diff utility, `AuditService.record()`, `@Audited()` decorator + `AuditTrailInterceptor` for declarative per-controller opt-in, paginated per-record history endpoint, CSV export per record and per module. Wired PLT-1's login success/failure/lockout events directly into the audit trail (pre-authentication boundary, so this bypasses the decorator pattern by design) and added a `POST /auth/change-password` endpoint (using the `@Audited()` pattern) so password changes are also audited, per this session's explicit scope. Client: shared `HistoryTab` component. 63 server tests + 4 client tests, all passing. | new feature — no impact on existing validated functions. `AuthService` now depends on `AuditService`; `PermissionsGuard`/history endpoints unchanged for existing routes. |
| 2026-07-11 | PLT-3 | E-Signatures: `POST /esign/challenge` re-verifies the user's password (tenant-configurable credential type; PIN documented as a future extension pending a `pinHash` field on User) and issues a short-lived (≤2 min, hard-capped in code), single-use signing token (JWT with a `jti` nonce). `SignatureGuard` verifies + atomically consumes that token (unique index on `jti` — a replay fails the insert) and rejects signing attempts backed only by a valid session. Append-only `signatures` collection (extracted the append-only Mongoose hook set from PLT-2's `auditEvents` into a shared `applyAppendOnly()` plugin, reused by both) storing user, UTC timestamp, meaning (`SignatureMeaning` enum: Reviewed by / Approved by / Trained — read and understood / Verified by / QA Disposition), entity ref, and a canonicalized SHA-256 hash of the entity snapshot at signing (`hashEntitySnapshot`/`verifyEntitySnapshot`, with tamper-detection tests). Client: shared `SignatureDialog` (credential re-entry, error handling) and `SignatureManifest` (name/meaning/timestamp) components. Also fixed a latent test-infra bug: RTL's auto-cleanup never registered between tests because the client's Vitest config uses `globals: false` — added explicit `afterEach(cleanup)` in setup-tests.ts. 91 server tests + 8 client tests, all passing. | new feature — no impact on existing validated functions. `auditEvents`'s append-only enforcement now runs through the shared plugin (behavior unchanged, existing tests still pass). |
| 2026-07-11 | PLT-5, PLT-8 | Numbering + Tenant/User Admin: this session finally introduces the `Tenant` entity itself (previously a bare `tenantId` with no backing document). **PLT-5** — tenant-configurable numbering schemes (prefix, department token, zero-padding, yearly reset) formatting as SOP-QA-001 / EQP-0042 / TRN-2026-0113; atomic gapless sequence generation via `findOneAndUpdate` `$inc`/upsert against a unique (tenantId, entityType, departmentCode, year) counter key, proven by a 50-concurrent-request test. Found and fixed a real (not just test) race: Mongoose builds indexes asynchronously in the background, so a brand-new counter's very first concurrent requests could beat the unique index into existence — fixed via an `onModuleInit` hook that awaits index creation before the app accepts traffic. **PLT-8** — `Tenant` schema with settings (timezone default Asia/Kolkata, e-sign credential type, session-timeout TTLs — JWT *secrets* remain env-only, never tenant-stored), `Department` master CRUD, `User` CRUD with role assignment, all audited via `@Audited()`. Tenant provisioning is platform-admin-only (`isPlatformAdmin` flag on User, orthogonal to the tenant-scoped permission matrix) via a new `PlatformAdminGuard`; the very first platform admin must be seeded directly into the database (documented operational bootstrap step, not an HTTP endpoint). `AuthService`/`EsignService` now resolve JWT-TTL/credential-type settings from the tenant document when present, falling back to the platform env defaults otherwise. Client: Tenants (super-admin only), Departments, Users, and Numbering Schemes admin pages, wired into nav/router with a `PlatformAdminRoute` guard. 120 server tests + 11 client tests, all passing. | new feature — no impact on existing validated functions. `AuthService`/`EsignService` constructors now also depend on the `Tenant` model (existing tests updated accordingly); User schema gained `isPlatformAdmin`/`departmentId` fields (both optional/defaulted, no migration needed). Password policy and lockout thresholds remain platform-env-configured, not yet tenant-configurable — narrower than earlier sessions' comments implied; flagged for a future session if needed. |
| 2026-07-11 | PLT-4 | Workflow Engine: generic, tenant-configurable approval engine usable by any business entity via a polymorphic `entityType`/`entityId` pair. `WorkflowTemplate` holds an ordered list of steps, each with a role-based assignee (`roleId`), a required `SignatureMeaning`, and a `rejectToStepIndex` (`null` = rejection returns the instance to the author's `DRAFT`; a number = re-enter `IN_PROGRESS` at that earlier step). `WorkflowInstance` tracks `status`/`currentStepIndex` and an optional `overrideAssigneeUserId` for reassignment. Actions — `submit`, `approve` (re-authenticated e-signature via PLT-3), `reject` (mandatory comment, zod `discriminatedUnion` enforces this per-action), `reassign` (admin-only, audited with a mandatory reason) — are dispatched through an explicit `WORKFLOW_ALLOWED_ACTIONS` transition map in `packages/shared`; invalid transitions throw before any state changes. Emits a `workflow.step-changed` event on every step change via `@nestjs/event-emitter`, with no listeners yet — reserved for PLT-6 Notifications. Endpoints: `GET /workflow/my-pending-tasks`, `GET /workflow/instances/:id`, `POST /workflow/instances/:id/act`, plus template CRUD. Refactored `SignatureGuard`/`EsignService` to expose `verifyAndConsumeSigningToken()` so the workflow engine can verify a single-use signing token in-process before writing its own `Signature` record (signatures are append-only, so a pre-existing signature can never be "attached" to a workflow after the fact). Extended `@Audited()`/`AuditTrailInterceptor` to accept a per-call `entityType`/`action` override, since one generic controller now writes audit events against many different business-entity types. Client: `PendingTasksPage`, `WorkflowStepper`, and a `WorkflowInstancePage` that reuses the (now control-inverted) `SignatureDialog` for approvals and a plain comment form for rejections. 142 server tests + 18 client tests, all passing (including full-happy-path with two real signatures, reject-back-and-reapprove, non-assignee-blocked, and reassign-audited-with-reason). Also fixed a Vite/Rollup production-build failure: Rollup's CJS-interop analysis can't statically resolve a plain `const` (`SIGNATURE_MEANING_LABELS`) through `packages/shared`'s multi-level `__exportStar` barrel re-exports, so the client now aliases `@pharmaqms/shared` straight to its TS source in `vite.config.ts` (the server is unaffected — it still consumes the compiled CJS `dist` via `require`). Live-verified end-to-end against the real Docker Mongo/Redis instances (not just automated tests): seeded a bootstrap platform admin, provisioned a real tenant, created real Dept Head/QA Head/Substitute users, and drove all three flows over real HTTP with real bcrypt-verified signature challenges. | new feature — no impact on existing validated functions. `SignatureGuard` behavior is unchanged from the caller's perspective (same request shape, same rejection cases); `@Audited()`'s new `entityType`/`action` override fields are optional and default to the decorator's static values, so all existing `@Audited()` call sites are unaffected. There is still no HTTP surface for creating `Role` documents beyond the one auto-created at tenant provisioning — additional roles (e.g. Dept Head, QA Head) must be seeded directly in the database until a future session adds role management endpoints; flagged for follow-up. |
| 2026-07-11 | PLT-6 | Notifications + Jobs: BullMQ (Redis) background-job infrastructure and a template-based notification service (SPEC.md §6.1 PLT-6 events: assigned / due-soon / overdue / approved / rejected). **Notification log** — per-user `notifications` collection (creation audited via direct `AuditService.record`; content write-once, only `isRead`/`emailedAt` flags mutate; no delete path), with a partial unique (tenantId, userId, dedupeKey) index as the atomic idempotency check. Endpoints: `GET /notifications` (paginated, `unreadOnly` filter), `GET /notifications/unread-count`, `POST /notifications/mark-read` (audited, own-notifications only). **Workflow fan-out** — `WorkflowNotificationListener` subscribes to PLT-4's step-changed events: task_assigned to the now-current step's role users (or the reassigned substitute only), approved/rejected routed back to the submitting author. To support this, `WorkflowInstance` gained `submittedByUserId`, `WorkflowService.submit()` now takes the actor and emits the step-changed event (previously only approve/reject emitted), reassign now also emits, and the shared event type gained `toStepRoleId`/`toStepName`/`overrideAssigneeUserId`/`submittedByUserId`. **Due-date scanner framework** — `DueDateScannerRegistry` (modules register scanners; duplicate keys rejected) + `DueDateScanService.runDailyScan()`: runs each scanner once per tenant per tenant-timezone calendar day (unique (tenantId, scannerKey, runDate) scan-run record = idempotent daily runs; per-finding dedupeKeys additionally make racing runs harmless). No scanners registered yet — DOC-6/TRN-5/EQP-4/EQP-9 will register theirs. **Email** — provider-agnostic `Mailer` interface with console (default dev) and file (NDJSON outbox) transports; per-tenant digest option (`Tenant.settings.notificationEmailMode`: immediate per-event emails via a BullMQ email queue, or one daily digest per user batching everything unsent). **Jobs** — `@nestjs/bullmq` + `bullmq` deps; two queues (`plt6-email`, `plt6-daily`) with thin `WorkerHost` processors delegating to services; repeatable daily due-date-scan and digest jobs scheduled at boot (crons env-configurable). NotificationsModule composes dynamically: `JOBS_ENABLED=false` (set for the whole Jest suite via `setupFiles`) swaps the BullMQ enqueue port for a no-op so tests never need Redis. Client: `NotificationBell` in the desktop shell header — unread badge (30s poll), dropdown unread list, mark-all-read. 25 new server tests + 2 client tests (167 server / 20 client total, all passing). Live-verified against real Docker Mongo+Redis: task_assigned/approved/rejected notifications over real HTTP, immediate email delivered through a real BullMQ worker to the file outbox, digest mode suppressing immediate email and the digest job batching it, both repeatable jobs present in Redis, and the due-date-scan job processed by the real worker. | new feature — no impact on existing validated functions. `WorkflowService.submit()` signature now requires the acting user (all call sites updated); the workflow step-changed event payload gained four fields (additive). `Tenant.settings` gained `notificationEmailMode` (defaulted, no migration needed). Server tests now set `JOBS_ENABLED=false` globally via Jest `setupFiles`. Emails are best-effort/fire-and-forget by design — a Redis outage degrades email delivery but never blocks the audited request path; the notification log (Mongo) is the durable record. |
| 2026-07-11 | PLT-7 | QR Service + Mobile Shell: opaque, collision-safe short codes (10-char unambiguous base32 subset, crypto-random, global unique index with regenerate-on-collision retry — proven by a forced-collision test) mapping /s/:code to a polymorphic entity ref; one code per entity per tenant (idempotent get-or-create, minting audited). Endpoints: POST /qr/codes (admin; modules mint in-process via QrService), GET /qr/resolve/:code (any authenticated user, tenant-scoped — a foreign code is indistinguishably 404, cross-tenant scan test included), GET /qr/codes/:code/png (server-generated via qrcode npm), GET /qr/codes/:code/label.pdf?size=single|a4 (puppeteer HTML->PDF: single 62x29mm adhesive label and an A4 cut-out grid of 27 copies; label shows QR + entity code + name from display snapshots stored at minting). New shared PdfRenderService (lazy singleton headless browser, common/pdf) — reused by DOC-4 controlled copies later. QR images encode {APP_BASE_URL}/s/{code} (new env var). Client: client/src/mobile — phone-first MobileShell with persistent "Logged in as {name}" banner and fast user-switch (SPEC §7.3 / §5.3 no-shared-logins), /s/:code scan landing resolving to entity-type-specific views (generic stub card until Phase 1 modules land), and login now honours a ?redirect= param (relative paths only — open-redirect guarded) so the scanned target survives login/user-switch. 13 new server tests + 3 client tests, all passing (label-PDF test renders through real puppeteer). | new feature — no impact on existing validated functions. Adds puppeteer + qrcode server deps. Login page behaviour unchanged except the new optional redirect param. |
| 2026-07-11 | N/A (SPEC §8 Phase 0 gate — integration demo) | Phase 0 definition-of-done: throwaway TestRecord demo entity (server/src/demo — deliberately outside the phase-gated server/src/modules tree; deleted once DOC/TRN/EQP land) exercising every platform service together: PLT-5 numbering at creation (TR-0001), PLT-7 QR minted at birth (label PDFs + /s/:code mobile view), audited create/edit with field-level diffs (PLT-2), 2-step approval via a PLT-4 template with two real e-signatures (PLT-3, session-only signing rejected), task-assigned + approved notifications (PLT-6), all under RBAC (PLT-1) and tenant isolation (PLT-8). Added WorkflowService.findInstanceForEntity() read-time join (needed by every entity detail page). Client: Test Records list/detail pages composing WorkflowStepper + SignatureManifest + HistoryTab + QR block. ONE end-to-end journey test (9 stages, nothing mocked) + live HTTP verification against real Mongo/Redis (real puppeteer label PDF, real BullMQ email). Demo replay steps documented in validation-pack/docs/phase0-demo.md. GATE PASSED — module sessions may begin. | new feature — no impact on existing validated functions. TestRecord is explicitly throwaway and excluded from the validated scope; known gaps flagged in phase0-demo.md (no roles/template admin UI yet). |
| 2026-07-11 | DOC-1, DOC-2, DOC-8 | Documents module core (first Phase 1 business module — gate passed): Document + DocumentVersion Mongoose schemas (DOC-1 metadata: PLT-5 number per doc-type scheme with department token e.g. SOP-QA-001, title, type SOP/Spec/Protocol/Format/Policy, department, review frequency, author; PDF/DOCX upload ≤20MB per version). Lifecycle as an explicit transition map in packages/shared (draft→under_review→under_approval→effective→superseded|obsolete, +cancelled for never-submitted drafts — Iron Rule 3; terminal states allow NO transitions, draft can never jump to effective); DocumentsService.transitionVersion() is the only state writer and every transition is audited. DOC-2: single-Effective invariant — making a version Effective auto-Supersedes the prior Effective (own audited status change); major/minor bumps; one in-flight version at a time; prior versions read-only. DOC-8: change summary mandatory on every version after 1.0 (zod at the edge + service re-check), stored as the version_created audit reason. Files: provider-agnostic FileStorage seam (common/storage — local-disk write-once implementation for dev/test, S3 adapter point for prod per SPEC §6); keys immutable per version. Document-level status derived from version states (under_revision = effective + new in-flight). Service + API + tests only per session scope — UI arrives with DOC-3. 19 new server tests including the §7.1 v-next-effective-auto-supersedes acceptance criterion, tenant isolation, permission denial, multipart e2e. | new feature — no impact on existing validated functions. Adds @types/multer; AuditAction gained version_created and controlled_copy_printed (additive). |
| 2026-07-11 | DOC-3, DOC-6, DOC-7 | Documents workflows + UI. **DOC-3** — version approval runs through PLT-4: POST /documents/:id/versions/:versionId/submit starts the configurable review→approval flow (each step an e-signature with its meaning, per §7.1); a DocumentWorkflowListener syncs version lifecycle state from workflow events THROUGH the shared transition map (submit→under_review, step-advance→under_approval, final approval→effective with DOC-2 auto-supersede, reject→draft or back to review with the comment as the audited reason). §7.1 acceptance criterion covered end-to-end: rejection with comment returns the version to the author as Draft and who/when/why lands in the audit trail; the same version is resubmittable. **DOC-7** — POST /documents/:id/obsolete: SignatureGuard-enforced fresh e-signature (QA Disposition meaning) + mandatory reason; obsolete documents leave default search/lists but remain reachable with includeObsolete=true and fully auditable. **DOC-6** — documents.periodic-review scanner registered into the PLT-6 daily-scan framework (due-soon within 30 days / overdue findings to the document author, dedupe keys stable across daily runs); GET /documents/review-due dashboard feed; outcome endpoints: reaffirm = e-signed (Reviewed-by) minor version sharing the same immutable file + review clock reset, revise = the normal new-version draft. NotificationsModule became global so modules can register scanners without re-running its dynamic Bull setup. Client: Documents register (filters, obsolete toggle), create form (metadata+file), detail page (version history, WorkflowStepper + approval-task link, merged SignatureManifest, HistoryTab, e-signed obsolete action via SignatureDialog), new-version form (mandatory change summary), Review-Due page (reaffirm via SignatureDialog / revise link). 10 new server tests (29 DOC total) + 2 client tests. | new feature — no impact on existing validated functions. Reaffirm deliberately bypasses the approval workflow (it is the "nothing changed" attestation, e-signed) — flagged for QA review of that interpretation of §7.1 DOC-6. |
| 2026-07-11 | DOC-4, DOC-5 | Controlled copies + QR version check. **DOC-4** — GET /documents/:id/versions/:versionId/controlled-copy.pdf renders the EFFECTIVE version as a controlled copy: pdf-lib stamps every page of the uploaded PDF with a header band (doc no. • version • effective date), the footer watermark "Controlled Copy — verify current version by scanning QR", and the version-check QR; DOCX sources get a puppeteer-rendered controlled cover sheet instead (v1 limitation — office files cannot be stamped in place; documented). Every print is audited (controlled_copy_printed: who printed which version, when). The QR minted for the copy identifies the exact VERSION, so stale prints scan as obsolete forever. **DOC-5** — GET /public/doc-check/:code (@Public — no login, no PII; the opaque code is the capability): returns CURRENT (version + effective date) or OBSOLETE (+ the current version number). QrService gained findByCodePublic() with the entity-type allow-listing done by the consumer (only DocumentVersion is public). Client: /s/:code now tries the public check first — a scanned printed copy shows the big ✔ CURRENT / ✘ OBSOLETE card with zero login friction, "log in to open the document" only for the full record; detail page gained a Print-controlled-copy action. §7.1 acceptance criterion covered: after v2.0 becomes Effective, scanning the printed v1.0 copy shows OBSOLETE with current=v2.0 (unauthenticated), and v2.0 scans CURRENT. 5 new server e2e tests + 2 client tests (34 DOC server tests total). | new feature — no impact on existing validated functions. Adds pdf-lib. The public endpoint exposes docNumber/version/effective date and an opaque documentId only — reviewed as no-PII per §7.1. |
| 2026-07-11 | TRN-1, TRN-2, TRN-3, TRN-4, TRN-5, DOC-9 | Training module (SPEC.md §7.2). **DOC-9** — Document gained distributionRoleIds/distributionDepartmentIds (which roles/departments must be trained) with its own PATCH /documents/:id/distribution endpoint, edited from the document detail page (natural ownership). **TRN-1** — a training matrix is a READ MODEL (DocumentTrainingTarget) mirrored purely off two broadcast events — DocumentTrainingTargetChangedEvent (fired on every distribution edit AND every DOC-3/DOC-6 effective-version transition, carrying a full docNumber/title/version/distribution snapshot) and UserRoleAssignedEvent (fired by PLT-8s UserAdminService on user creation and on any role/department change) — so Training never imports the Documents module directly (CLAUDE.md: business modules never depend on one another). Adding a user to a mapped role, or mapping a role to an already-Effective document, both auto-generate a pending assignment. **TRN-2** — read-and-understood: SignatureGuard-protected completion e-signs Trained — read and understood (own-assignment only, Iron Rule 4 — a bare session cannot complete anothers or ones own task without a fresh credential). **TRN-3** — a new Effective version retargets an OPEN pending assignment in place (no duplicate row) or, if the prior version was already completed, creates a fresh pending row (status flips to training due) while the completed row is never touched (Iron Rule 3 — permanent history). **TRN-4** — puppeteer-rendered per-employee training record (JSON + PDF), self-service or training:view for others. **TRN-5** — a documents.overdue-training-style scanner (training.overdue-assignments) registered into the PLT-6 daily-scan framework notifies BOTH the employee and their department head (Department gained headUserId) as two independent findings sharing one dedupeKey; grace period is a new tenant setting (trainingGracePeriodDays, default 7, resolved like DOC-6s review frequency so a tenant-wide change retroactively re-derives every pending due date). Client: My Trainings (mobile-first cards), Training Matrix (admin overview linking to each document), Overdue dashboard, Employee Record page, and the distribution checklist added to the document detail page. 24 new server tests (2 unit suites + 1 cross-module e2e proving the whole DOC-9->TRN-1->TRN-2->TRN-3 chain through the REAL Documents module, nothing mocked) + 3 client tests. Live-verified against real Mongo/Redis: the full chain plus BullMQ-driven overdue notifications to two distinct recipients. | new feature — no impact on existing validated functions. UserAdminService and DocumentsService constructors now also depend on EventEmitter2 (both already-global in AppModule; two existing unit-test modules needed EventEmitterModule.forRoot() / a mocked EventEmitter2 added — fixed, no behavior change). Department gained headUserId (optional/defaulted); Tenant gained trainingGracePeriodDays (defaulted). AuditAction gained distribution_updated/training_assigned/training_completed (additive). |
| 2026-07-12 | EQP-1, EQP-2, EQP-3 | Equipment master + QR + mobile status card (SPEC.md §7.3, flagship module). **EQP-1** — Equipment schema (code/name/make/model/serial/location/department/GMP-critical/status/install date; location is free-text in v1 — no Room master exists yet, QRX-1 is v1.5/out of scope) with an explicit status transition map in packages/shared (Active <-> Under Maintenance/Under Qualification, both -> Retired; Retired is terminal, Iron Rule 3 — no un-retiring, a returned instrument gets a new record). PLT-5 numbers it (EQP-0001…). **EQP-2** — every equipment gets its QR minted at creation via the existing PLT-7 QrService (no new label-PDF code needed — /qr/codes/:code/label.pdf already serves single + A4-grid sizes from session 8). **EQP-3** — GET /equipment/:id/status-card (any authenticated user — the scan itself is the access gate, same as QR resolve) renders the live calibration indicator via a new pure deriveCalibrationStatus() function (VALID / DUE_SOON / OVERDUE / NOT_SCHEDULED, 30-day due-soon window) — built and fully tested now even though nothing feeds it a real due date until EQP-4; qualification/PM/logbook are honestly-labeled stubs (not_available / null / []) rather than invented data models for EQP-8/EQP-9/EQP-6, which are out of scope for this build phase. Role-driven action button stubs: any authenticated user sees log_usage/log_cleaning/report_breakdown (EQP-6/7 will wire these — SPEC says logging needs no elevated permission, just an authenticated scan); equipment:approve/equipment:edit holders additionally see record_calibration/complete_pm stubs. Client: Equipment register + create + detail (status transition buttons, QR label downloads via authenticated blob fetch — a plain anchor href cannot carry the JWT bearer token, caught before shipping — mandatory HistoryTab) and a new EquipmentStatusCard component wired into /s/:code's entity-type switch (equipment scans now render the real card; every other entity type still gets the Phase 1 stub). 23 new server tests + 6 client tests. Live-verified against real Mongo/Redis: full create -> QR mint -> real PNG/label-PDF rendering -> scan -> role-gated status card -> audited status transitions -> Retired terminal-state rejection -> RBAC denial -> tenant isolation (both the record AND its QR scan blocked). | new feature — no impact on existing validated functions. A pre-existing equipment schema field name collision with Mongoose's built-in Document.model() was caught at compile time and fixed by renaming to modelName (API-visible field name change, but nothing consumed the old name yet). |
| 2026-07-12 | EQP-4, EQP-5 | Calibration + out-of-tolerance handling (SPEC.md §7.3), built as a sub-concern of the Equipment module rather than a separate top-level business module (CalibrationService depends directly on EquipmentService — CLAUDE.md's "business modules never depend on each other" rule concerns separate top-level modules, not sub-concerns within one). **EQP-4** — one `CalibrationSchedule` per equipment (frequency, parameters, tolerance class, internal/external agency ref, next due date); `CalibrationRecord` per performed event with certificate upload (FileStorage, PDF/JPEG/PNG ≤20MB) and a QA `Verified by` e-signature for PASS results that advances `nextDueDate` by `frequencyMonths`. A new `EquipmentCalibrationScanner` registers into the PLT-6 daily due-date scan framework and notifies the equipment's department head (`Department.headUserId`, the same field TRN-5 introduced) — equipment has no per-employee "assigned technician" concept, unlike training. `EquipmentStatusCard`/`getStatusCard()` now feeds real calibration data instead of the EQP-3 stub, plus a new tenant setting `blockUsageWhenCalibrationOverdue` (default `true` — the safer GxP default) surfaced as a derived `calibrationBlocksUsage` flag (actual enforcement — blocking EQP-6 usage logging — is deferred to Session 16, which doesn't exist yet in this codebase). **EQP-5** — a FAIL/OOT result mandates an impact-assessment note (zod `.refine()` at the edge, re-checked in the service) and IMMEDIATELY flips the equipment to a new `DO_NOT_USE` status at record-creation time — before any QA action — since the risk exists the moment the instrument is known out-of-tolerance, not once QA gets around to a decision. `DO_NOT_USE` is structurally reachable/leavable in the shared transition map but the service layer additionally forbids the GENERIC `POST /equipment/:id/status` endpoint from entering or leaving it; only a dedicated `EquipmentService.setCalibrationLockStatus()` (called exclusively by `CalibrationService`) may do so. QA disposition (a second e-signature, meaning `QA Disposition`) resolves a FAIL record with outcome `release` (back to Active) or `retain_do_not_use`, plus a nullable `deviationRef` field (Phase 2 placeholder — no Deviations module exists yet). Disposition deliberately does NOT touch the schedule's `nextDueDate` — only a fresh PASS+VERIFIED record advances it, since a disposition is a risk decision, not a recalibration event. New endpoints: schedule create/get, record list/create (multipart), verify (`equipment:approve` + SignatureGuard), disposition (`equipment:approve` + SignatureGuard), and a QA-facing `GET /equipment/calibration/due` dashboard feed. Client: calibration schedule + record-result forms and a QA verify/disposition flow (two-step: note first, then the signature dialog — never both modals at once) on the equipment detail page, a `calibrationBlocksUsage`/`DO_NOT_USE` warning banner on the mobile status card, and a Calibration Due dashboard page. 8 new calibration-service unit tests + 5 scanner unit tests + 6 e2e tests (287 server tests total) + 9 new client tests (40 client tests total), all passing. Fixed one pre-existing test regression: `equipment-transitions.spec.ts` asserted Active's transition list before `DO_NOT_USE` existed. Live-verified against real Mongo/Redis: full schedule creation, PASS record + certificate upload + QA verification (with an engineer correctly forbidden by `equipment:approve` gating), FAIL/OOT record immediately quarantining the equipment (generic status endpoint correctly blocked), QA disposition release AND retain_do_not_use (with deviationRef), the calibration-due dashboard, and a real BullMQ-driven daily-scan notification reaching the department head. | new feature — no impact on existing validated functions. `EquipmentService`'s constructor gained `CalibrationSchedule`/`Tenant` models and `AuditService` (existing unit test module updated accordingly); `EquipmentStatusCardData` gained `calibrationBlocksUsage` (additive); `EquipmentStatus` gained `DO_NOT_USE` (additive enum value, existing transition-map assertions updated); `Tenant.settings` gained `blockUsageWhenCalibrationOverdue` (defaulted, no migration needed). The verify/disposition endpoints are gated by `equipment:approve` (not just SignatureGuard) — flagged as a deliberate correction during this session: an initial draft relied on the signature challenge alone, which would let ANY authenticated user with a valid password verify/disposition a calibration record regardless of role; fixed before this changelog entry was written. |
| 2026-07-12 | EQP-6, EQP-7 | Digital logbook + maintenance tasks (SPEC.md §7.3), completing the flagship EQP module — built as sub-concerns of the Equipment module (`LogbookService`/`MaintenanceService` depend on `EquipmentService` and each other directly, same architectural precedent as EQP-4/5's `CalibrationService`). **EQP-6** — an authenticated scan logs `usage_start`/`usage_stop` (free-text product/batch ref), `cleaning` (routine/full), or `breakdown` (description + optional JPEG/PNG photo, ≤10MB) against the equipment; every entry carries the server UTC timestamp and the acting user, and the `logbookEntries` collection is schema-level append-only (the shared `applyAppendOnly()` plugin from PLT-2/PLT-3 — no service method, and no direct Mongoose call, can update or delete an entry). Corrections are logged as a brand-new `amendment` entry referencing the original via `amendsEntryId` — never an edit; the client renders the original alongside its correction rather than a strike-through in place, since the original is immutable and unreachable for in-place styling changes anyway. A usage session must be started before it can be stopped (one open session at a time, enforced by walking the most recent usage-type entry) and `usage_start` is the actual EQP-4 enforcement point Session 15 built `calibrationBlocksUsage` ahead of: blocked outright when the equipment is `DO_NOT_USE` or `Retired`, and blocked when calibration is OVERDUE and the tenant's `blockUsageWhenCalibrationOverdue` setting is true (default) — verified end-to-end against a real OOT-quarantined equipment. `EquipmentService.getStatusCard()` now feeds the real last-5 logbook entries (previously an honest `[]` stub) and its four independent lookups (equipment, calibration schedule, tenant settings, recent entries) were parallelized via `Promise.all` for the SPEC §7.3 <2s scan-to-card target — live-measured at ~32ms locally end-to-end (resolve + status-card), documented as a floor rather than a network-representative number. **EQP-7** — a `breakdown` entry immediately auto-creates a `MaintenanceTask` (OPEN), assigned to a new tenant-configurable "maintenance role" setting (`Tenant.settings.maintenanceRoleId`, mirroring TRN-5/EQP-4's role/department-based notification precedent — SPEC's "assigned to maintenance role" read literally as a Role reference) and notifies every active user holding that role (task_assigned, same fan-out pattern as PLT-4's `WorkflowNotificationListener`, called directly here since Maintenance is a same-module sub-concern rather than a cross-module listener). Engineer closure (`equipment:edit`, mandatory completion note) moves the task to `PENDING_VERIFICATION` or straight to `CLOSED` depending on a second new tenant setting, `requireMaintenanceVerification` (default `true`, same safer-GxP-default precedent as EQP-4) — both settings are snapshotted onto the task at creation so a later tenant-settings change never silently reinterprets an in-flight task. QA/user verification reuses the `Verified by` signature meaning (no new enum value) and requires `equipment:approve` + SignatureGuard, matching EQP-4's calibration-verify gating exactly. New endpoints: `POST/GET .../logbook/{usage-start,usage-stop,cleaning,breakdown}`, `POST .../logbook/:entryId/amend`, `GET .../logbook`, `GET .../logbook/:entryId/photo`, `GET .../maintenance-tasks` (per-equipment) + `GET /equipment/maintenance-tasks/open` (QA-facing queue) + `POST .../maintenance-tasks/:taskId/{close,verify}`. Client: real Start/Stop Usage, Log Cleaning, and Report Breakdown actions replace EQP-3's disabled stub buttons on the mobile status card (record_calibration/complete_pm remain disabled — a multi-field form with a certificate upload is a poor fit for a phone-first card and is already handled on desktop), a correction dialog wired to the amendment flow, and photo viewing via an authenticated blob-fetch (the same pattern as QR label/certificate downloads — a plain `<img>`/`<a>` can't carry the JWT bearer token). Desktop equipment detail page gained a read-oriented `LogbookPanel` and a `MaintenanceTaskPanel` (close/verify, permission-gated the same as the mobile card), plus a new Maintenance Queue dashboard page. 14 new server unit tests (8 logbook + 6 maintenance) + 10 e2e tests (312 server tests total) + 15 new client tests (66 client tests total), all passing. Live-verified against real Mongo/Redis: usage start/stop pairing and rejection of an out-of-order stop/double-start, cleaning, a real photo upload + retrieval, the auto-created maintenance task notifying a real distinct maintainer user, an amendment leaving its original entry byte-for-byte unchanged, engineer-closes/QA-verifies with the engineer correctly forbidden from verifying, the closed task leaving the open queue, a real OOT-quarantined equipment blocking usage-start, tenant isolation, and the scan-to-card timing measurement above. | new feature — no impact on existing validated functions. `EquipmentService`'s constructor gained the `LogbookEntry` model (existing calibration/equipment unit test modules updated accordingly); `EquipmentStatusCardData.recentLogbookEntries` narrowed from `unknown[]` to `LogbookEntryData[]` (additive in practice — no consumer relied on the stub shape). `Tenant.settings` gained `maintenanceRoleId` (nullable, defaulted `null`) and `requireMaintenanceVerification` (defaulted `true`) — no migration needed. `AuditAction` gained `logbook_entry_logged`/`maintenance_task_created`/`maintenance_task_closed`/`maintenance_task_verified` (additive). This closes out the EQP flagship module's P0 scope (EQP-1 through EQP-7) from the user's original multi-session request; EQP-8 (qualification records) and EQP-9 (preventive maintenance) remain out of scope for this build phase per their own P0 status but no prior session's stub language promised them here. |
| 2026-07-12 | EQP-8, EQP-9 | Qualification records + preventive maintenance (SPEC.md §7.3), closing out the EQP flagship module's full P0 scope — again built as sub-concerns of the Equipment module (`QualificationService`/`PmService` depend directly on `EquipmentService`; `EquipmentService` in turn takes the `QualificationRecord`/`PmPlan` Mongoose models directly, NOT the two new services, to avoid a circular dependency — the shared derivation logic those two services also need lives in a standalone pure-function file, `qualification-summary.util.ts`). **EQP-8** — IQ/OQ/PQ/Requalification records with a mandatory protocol upload (report optional at creation, attachable exactly once afterward) and a PASS result may carry a `requalificationFrequencyMonths`, which the shared `deriveQualificationStatus()` combines with the most recent PASS's performed date into QUALIFIED / DUE_SOON / OVERDUE / NOT_QUALIFIED for the status card. A new `EquipmentQualificationScanner` registers into the PLT-6 daily due-date scan framework and notifies the department head, same recipient precedent as EQP-4/EQP-7. Recording a qualification event is deliberately NOT an e-signature: SPEC's EQP-8 line item never mentions one (unlike EQP-4's `Verified by` or EQP-9's completion below), so this is a plain `equipment:edit`-gated form, matching how EQP-4's raw calibration-result recording (pre-verification) is also un-signed — flagged in case QA review of the finalized SPEC wants qualification recording itself signed as evidentiary. **EQP-9** — one `PmPlan` per equipment (frequency months + free-text checklist) drives an auto-generated `PmTask` when overdue; unlike EQP-7's breakdown-triggered `MaintenanceTask` (created synchronously, in-request), a PM task is generated by a NEW kind of scanner — `EquipmentPmScanner` both notifies AND mutates data as a side effect of the daily scan, guarded at two independent layers so a re-run (whether the outer per-`(tenant, scannerKey, day)` scan-run guard, or the task's own unique `(tenantId, planId, dueDate)` index) can never duplicate a task. Completion is a single-step e-signature (`PM Completed by`, new `SignatureMeaning` value) gated to `equipment:edit` — a deliberate, narrower interaction than EQP-7's two-step engineer-closes/QA-verifies maintenance flow, since SPEC describes PM completion as one signed action with no separate verification step; flagged for QA review if a verification step is later wanted to match EQP-7. Completing a task advances the plan's `nextDueDate` by its frequency, mirroring EQP-4's calibration-record-advances-schedule pattern. `EquipmentService.getStatusCard()` now feeds real `qualificationStatus`/`qualificationNextDueDate`/`pmStatus` (previously EQP-3's honest `not_available`/`null` stubs), computed via two parallelized lookups alongside the pre-existing calibration/logbook ones. New endpoints: `POST/GET .../qualification-records`, `POST .../qualification-records/:recordId/report`, `GET .../qualification-records/:recordId/{protocol,report}`, `POST .../pm-plan`, `GET .../pm-plan`, `GET .../pm-tasks`, `GET /equipment/pm-tasks/open` (QA-facing queue), `POST /equipment/pm-tasks/:taskId/complete`. A real production fix found along the way (same class of bug as PLT-5's numbering counter, Session 11): `PmTask`'s unique index is built asynchronously by Mongoose, so `PmService` now implements `OnModuleInit` and awaits `taskModel.init()` before accepting traffic, same fix pattern as `NumberingService`. Client: a `QualificationPanel` (record form + records list + protocol/report view + attach-report dialog) and `PmPanel` (plan create/update + task list + two-step completion dialog: note first, then `SignatureDialog`) added to the equipment detail page; mobile status card's qualification/PM stub text replaced with real derived status+date; new PM Queue dashboard page and nav link. 28 new server unit/scanner tests + 5 e2e tests (345 server tests total) + 6 new client tests (72 client tests total), all passing. Live-verified against real Mongo/Redis/BullMQ: IQ then PQ-PASS-with-frequency, status card reporting QUALIFIED with a requalification due date, one-time report attach (second attempt rejected), an already-overdue PM plan, one real daily-scan run notifying for BOTH overdue requalification and overdue PM AND auto-generating the PM task, a genuine re-run of the PM scanner (scan-run record deleted first) proving the task-level unique-index guard rather than only the outer scan-run guard, QA (equipment:approve, no equipment:edit) correctly forbidden from completing a PM task, engineer completion with a real e-signature advancing nextDueDate, the completed task leaving the open PM queue, and tenant isolation on both new record types. Also fixed a real jsdom/vitest-only gap discovered while testing the qualification form: reading an uploaded file via `new FormData(formEl).get('fieldName')` unreliably returns `null` in this test environment even after `user.upload()` correctly sets the input's `.files`; worked around by reading `.files` directly off the DOM element instead (kept `FormData` for the plain text/number fields) — believed jsdom-specific, not retrofitted into the pre-existing, uncovered calibration/breakdown-photo forms since those are proven correct via live HTTP instead. | new feature — no impact on existing validated functions. `EquipmentService`'s constructor gained the `QualificationRecord`/`PmPlan` models (existing calibration/logbook/equipment unit test modules updated accordingly). `EquipmentStatusCardData.qualificationStatus` changes type from a literal stub (`'not_available'`) to the real `QualificationStatus` enum, and gains `qualificationNextDueDate`/`pmStatus` (all existing mock fixtures asserting the old stub shape were updated — an API-visible field-value change for any consumer that read the old literal, but nothing outside this codebase does yet). `SignatureMeaning` gained `PM_COMPLETED` and `AuditAction` gained `QUALIFICATION_RECORDED`/`QUALIFICATION_REPORT_ATTACHED`/`PM_PLAN_UPSERTED`/`PM_TASK_GENERATED`/`PM_TASK_COMPLETED` (all additive). This closes the EQP flagship module's full P0 scope (EQP-1 through EQP-9) from the user's original multi-session request. |
| 2026-07-12 | EQP-10 (P1); DOC-6/TRN-5/EQP-4/PLT-4 (dashboard consolidation); PLT-2 (export UI) | Dashboards + reports (SPEC.md §8 items 13-14). **QA home dashboard** — replaces the Phase-0 `ScaffoldHomePage` at `/` with a real client-side aggregation of four dashboard-feed endpoints that already existed from prior sessions (no new backend endpoint): PLT-4's `my-pending-tasks` (no permission gate — matches the endpoint's own no-extra-permission design), EQP-4's `calibration/due` (client-filtered to OVERDUE only, per the session brief's literal wording — DUE_SOON entries stay on the full Calibration Due page), TRN-5's `training/overdue`, and DOC-6's `documents/review-due`; each of the latter three widgets is gated on the same permission (`equipment:view`/`training:view`/`documents:view`) its own full-list page already requires and simply doesn't render if the user lacks it, same pattern as the existing platform-admin nav-link gate. **EQP-10** — a new `GET /equipment/:id/history-report.pdf` endpoint (`EquipmentHistoryReportService` + `EquipmentHistoryController`, a new sub-concern of the Equipment module following the qualification/PM precedent) aggregates the full lifecycle — qualification records, calibration schedule/records, PM plan/tasks, logbook entries (including breakdowns), and maintenance tasks — via `Promise.all` across every existing EQP-4/6/7/8/9 sibling service (all already exported from `EquipmentModule` for exactly this), rendered through the same `PdfRenderService`/puppeteer seam as TRN-4/DOC-4 (`EquipmentModule` gained a `PdfModule` import it didn't need before). `equipment:view` gated, no signature — it's a read report, nothing is being attested. **Audit-trail export UI** — PLT-2's per-record and per-module CSV export endpoints (built in Session 2, fully tested, but never wired to any UI) now have a real front door: a new shared `ExportAuditButton` component (one place owning the blob-fetch-and-download logic for both modes) is mounted inside `HistoryTab` itself (per-record export, so it now appears on every regulated detail page automatically — Document/TestRecord/Equipment/WorkflowInstance) and as a standalone "Export audit history (CSV)" action on each business module's list page (Documents, Equipment, Training Matrix — TestRecords' list page was deliberately left alone since it's the explicitly-throwaway Phase 0 demo, excluded from validated scope per Session 15's changelog note). **HistoryTab coverage audit** — confirmed via a full grep across `client/src/features` that every true single-entity detail page already had one (Document/TestRecord/Equipment) except `WorkflowInstancePage`, which gained `<HistoryTab entityType={instance.entityType} entityId={instance.entityId} />` — deliberately the underlying business entity's history, not a "WorkflowInstance" history, since `WorkflowController.submit()`/`actOnStep()` already audit against the underlying entity (`after.entityType`/`after.entityId`), not the instance's own id; this is exactly the history an approver would otherwise have to navigate away to see. `EmployeeRecordPage` was assessed and deliberately NOT given a `HistoryTab`: it aggregates many `TrainingAssignment` histories for one user, not one entity by id, so `HistoryTabProps`'s single-`entityId` contract doesn't structurally apply — same reasoning that already correctly excludes the PM/Maintenance queue pages. 6 new server tests (aggregation unit tests + PDF/permission/tenant-isolation e2e smoke tests, 353 server tests total) + 8 new client tests (QA dashboard, ExportAuditButton, HistoryTab export wiring, WorkflowInstancePage HistoryTab, EQP-10 download button — 72 client tests total), all passing. Live-verified against the real running server/Mongo/Redis: a real qualification record + overdue calibration schedule feeding both the QA dashboard's overdue-calibrations widget and a real multi-page PDF (verified non-trivial size and `%PDF-` magic bytes, not just a blank page), the no-permission user correctly denied the report, all four dashboard feeds reachable, both audit CSV export routes returning real multi-row CSVs, and tenant isolation on both the PDF endpoint (404) and the per-record CSV export (silently empty, not leaked). | new feature — no impact on existing validated functions. `EquipmentModule` gained a `PdfModule` import and two new providers/controllers (additive). `HistoryTab` and three module list pages gained a new button; no existing props or behavior changed. The QA home dashboard replaces dead scaffold copy at `/` — no prior functionality existed there to regress. |
| 2026-07-12 | Hardening pass (SPEC.md §8) — no new requirement IDs; strengthens PLT-1/PLT-3/PLT-4/PLT-5 and Iron Rules 3/4/5 across all P0/P1 modules | Full validation hardening pass: ran the complete suite, generated requirement traceability, and audited tenant isolation / guard bypass / hard-delete discipline end-to-end, fixing every gap found. **Traceability** — new `scripts/generate-traceability.js` parses SPEC.md §6.1/§7's requirement tables and cross-references every test `describe`/`it` name across all 84 test files, writing `validation-pack/traceability/traceability-matrix.md` + a JSON companion; confirms all 29 P0 requirements (PLT-1…8, DOC-1…7, TRN-1…5, EQP-1…9) have test coverage — zero gaps. **Genuine security gap found and fixed**: the generic `POST /workflow/instances/submit` endpoint (PLT-4) had no permission check tied to the target entityType, so a user with zero permission on Documents could submit a DocumentVersion into its approval workflow directly, bypassing DOC-3's `documents:edit`-gated wrapper entirely (real side effects: spurious reviewer notifications, a misattributed audit event, and consuming the DRAFT→IN_PROGRESS transition so the legitimate author's later submit would fail). Fixed via a new `WORKFLOW_SUBMIT_ENTITY_TYPE_PERMISSION` map in `packages/shared` (plain string keys, not a business-module import, to keep the platform layer decoupled) checked in `WorkflowController.submit()`; regression test added. **Tenant-isolation test gaps found and fixed**: Documents had no e2e isolation test (only a read-only unit one) despite being the highest-risk module — added one covering GET/list/download/write; `platform/esign` had zero isolation tests despite an endpoint structurally identical to audit's well-tested one — added one; `platform/workflow`'s existing "tenant-isolated" e2e test was confounded (passed only because the outsider had no template configured, never proving denial of a real pre-existing instance) — strengthened to actually hit GET/act on a real cross-tenant instance id. Remaining test-depth gaps (numbering has no e2e suite at all; equipment's due-date scanners fully mock the DB so nothing proves their tenantId filter; most equipment sub-concerns test read-only cross-tenant denial, not writes) are documented as follow-ups in `validation-pack/docs/v1-readiness.md`, not fixed this session — in every case the underlying implementation was independently verified correct via the guard audit, only test depth is short. **Hard-delete audit**: zero hard-delete calls exist anywhere in production code (Iron Rule 3 holds with more conservatism than required — the "never-submitted draft" exception has no live code path at all); confirmed `applyAppendOnly()` hard-blocks delete/update at the Mongoose layer for `auditEvents`/`signatures`/`logbookEntries`, and flagged (as a recommendation, not a fix) that the other 18 regulated schemas rely on discipline rather than a schema-level guarantee. **Load test**: 200 concurrent `/qr/resolve/:code` → `/equipment/:id/status-card` round trips (the real `/s/:code` scan flow), 0 errors, p99 717ms — comfortably under SPEC §7.3's 2s target as a local floor measurement. Two minor documentation-only gaps closed (`EsignController.listSignatures`, `WorkflowController.getInstance` now explain their auth-only gating, matching the pattern used everywhere else). 3 new server tests (353 total) — all fixes are additive/corrective with zero new business functionality. | bug fix (real permission-bypass gap in PLT-4's generic submit endpoint) + hardening (test-coverage gaps closed) + new tooling (`scripts/generate-traceability.js`, `validation-pack/traceability/`, `validation-pack/docs/v1-readiness.md`). No impact on any other existing validated functionality — the workflow-submit fix only newly REJECTS a call pattern (`entityType: 'DocumentVersion'` from a caller lacking `documents:edit`) that was never a legitimate, intended use of the generic endpoint in the first place; every existing passing test (350 server, 64 client) continued to pass unchanged. |
